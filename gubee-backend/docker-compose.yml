version: '3'

services:
  postgres:
    image: postgres
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: gubee
      POSTGRES_PASSWORD: gubee
      POSTGRES_DB: gubeeapi

volumes:
  db_data: